Вы работаете в ботаническом исследовательском центре, который сотрудничает с садоводческими компаниями. Ваша задача — разработать инструмент для автоматического определения видов цветов ириса на основе их характеристик. Это поможет ускорить процесс сортировки растений для коммерческих поставок.

Для решения задачи требуется:

    Загрузить и разделить данные на обучающую и тестовую выборки.

    Исследовать влияние предобработки данных на качество модели.

    Обучить модель классификации и проанализировать её точность.

Примечание: указанные в условии параметры не всегда приводят к наилучшей точности обученной модели, но вам необходимо их соблюдать.

0. Зависимости
Никакие библиотеки при выполнении работы импортировать не нужно. Необходимые библиотеки импортированы следующим образом:

import numpy as np
import pandas as pd
from sklearn.datasets import load_iris
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler, MinMaxScaler, RobustScaler
from sklearn.tree import DecisionTreeClassifier
from sklearn.metrics import accuracy_score

1. Загрузка данных:

Требования:

Реализуйте функцию load_data(), которая:

    Загружает датасет Iris из библиотеки sklearn.datasets.

    Принимает аргумент train_size (по умолчанию 0.7) — доля данных для обучения.

    Разделяет данные на признаки (X) и целевую переменную (y).

    Выбирает первые три признака из датасета (столбцы 0, 1, 2).

    Разделяет данные на обучающую и тестовую выборки с помощью train_test_split:

        Параметр random_state=42 (фиксация случайности)

        Параметр stratify=y (сохранение распределения классов)

Возвращает:
X_train, X_test, y_train, y_test (тип возвращаемых значений - np.ndarray).

Пояснение:

    X_train и X_test — трёхмерные массивы (признаки), y_train и y_test — одномерные (метки классов).

2. Конвертация в Pandas-структуры

Требования:

При выполнении задания обращайтесь к библиотеке через сокращение pd. Реализуйте функцию convert_to_pandas(), которая:

    Принимает данные (X — массив признаков, y — массив меток).

    Преобразует X в pandas.DataFrame с названиями столбцов: sepal_length, sepal_width, petal_length.

    Преобразует y в pandas.Series с именем species.

Возвращает:
Кортеж (df, target_series).  Типы возвращаемых значений - pd.DataFrame и pd.Series.

3. Исследование данных средствами Pandas

Требования:

Реализуйте функцию explore_data(), которая:

    Принимает df (DataFrame) и target_series (Series).

    Выводит:

        Первые 5 строк df.

        Статистику по каждому признаку (минимум, максимум, среднее и т.д.) через df.describe().

        Распределение классов в target_series (количество объектов каждого вида).

Возвращает:
None (функция только выводит информацию).

4. Предобработка данных

Требования:

Реализуйте функцию preprocess_data(), которая:

    Принимает следующие данные:

        X_train - обучающие данные (признаки)

        X_test - тестовые данные

        scaler_type - тип скейлера (по умолчанию 'standard')

    Поддерживает типы скейлеров:

        'standard' → StandardScaler()

        'minmax' → MinMaxScaler()

        'robust' → RobustScaler()

        Для других значений функция возвращает None

    Выполняет:

        Инициализирует скейлер выбранного типа
        Обучает скейлер только на обучающих данных (fit_transform для X_train)

        Применяет обученный скейлер к тестовым данным (transform для X_test) 

Возвращает:
Кортеж (X_train_scaled, X_test_scaled) — обработанные данные. Тип возвращаемых в кортеже данных - np.ndarray. 

Важно:

    Тестовые данные должны трансформироваться с использованием параметров, полученных на обучающей выборке.

5. Обучение модели

Требования:

Реализуйте функцию train_model(), которая:

    Принимает X_train, y_train и параметр max_depth (по умолчанию 5).

    Создаёт модель DecisionTreeClassifier с заданным max_depth и параметром random_state=42

    Обучает модель на переданных данных.

Возвращает:
Экземпляр обученной модели.

6. Применение модели.

Требования:

Реализуйте функцию predict(), которая:

    Принимает два аргумента: обученную модель model и тестовые данные X_test.

    Выполняет предсказание меток для X_test с помощью метода model.predict().

Возвращает:
Одномерный массив с предсказанными метками (тип np.ndarray).

Пояснение:

    Для работы функции достаточно вызвать стандартный метод классификатора из sklearn.

7. Оценка качества модели

Требования:

Реализуйте функцию evaluate(), которая:

    Принимает два аргумента: предсказанные метки y_pred и истинные метки y_test.

    Вычисляет точность (accuracy) модели с помощью функции accuracy_score.

    Округляет результат до трёх знаков после запятой.

Возвращает:
Число в диапазоне [0, 1] (например, 0.923), тип значения - float.

Пояснение:

    Ответ 1.0 означает 100% точность, 0.0 — все предсказания неверны.

В jupyter notebook:

    Добавьте в функцию train_model doc-string, который кратко поясняет, на что влияет параметр max_depth.

    Добавьте в функцию evaluate doc-string, который кратко поясняет, что повлияло на точность модели.

В отчёте ответьте на следующие вопросы, опираясь на полученные вами результаты и код:

    В функции `evaluate` точность округляется до трёх знаков. Достаточно ли полученного значения `accuracy` для оценки качества классификации ирисов? В каких случаях её недостаточно и нужно смотреть на другие метрики (precision, recall, F1)?

    В функции `explore_data` выводятся первые 5 строк и статистика `describe()`. Какие отклонения от нормального распределения были замечены в признаках (например, асимметрия, выбросы)? Как поддерживаемые скейлеры (`StandardScaler`, `MinMaxScaler`, `RobustScaler`) обрабатывают такие особенности?



Ответ:(штрафной режим: 0 %)